version: '3.8'

services:
  app:
    image: openjdk:17-jdk
    container_name: springboot-app
    volumes:
      - ./target/app.jar:/app.jar
    command: ["java", "-jar", "/app.jar"]
    ports:
      - "8080:8080"
